import React from "react";
import { Layout } from "../components/Layout";
import { ChatList } from "../components/ChatList";

const Chat = () => {
  return (
    <Layout>
      <ChatList />
    </Layout>
  );
};

export default Chat;
